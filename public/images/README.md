Drop the room's real photography here, then update the paths in
`src/lib/content.ts` and `src/lib/events.ts` and remove the `remotePatterns`
block from `next.config.mjs`.

Suggested: room.jpg, about.jpg, founder.jpg, og.jpg (1200x630),
and events/<slug>.jpg for each tournament.
